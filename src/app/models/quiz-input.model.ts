import { Quiz } from "./quiz.model";

export class QuizInput extends Quiz<string> {
    controlType = 'textbox';
    type: string;

    constructor(options: {} = {}) {
        super(options);
        this.type = options['type'] || '';
    }
}